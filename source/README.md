# Private source handling

Toronto Notes 2025 remains outside version control. Configure its machine-local absolute path in the ignored `config/project.local.json` file.

Do not commit PDFs, extracted text, OCR output, copied prose, or source-derived artifacts. Private extraction data belongs under the Git-ignored `derived/` directory and must never be placed in deployable assets.
